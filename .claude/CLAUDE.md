# CatchFire Influencer Matching Engine

> Version: 0.8.1 | GCP Project: catchfire-app-2026 | Node.js 22+ | Express 5.x

AI-powered system for discovering and matching creators based on craft storytelling skills (not audience size). Feed a client brief in, get ranked creator recommendations based on style, passion, and location.

## Session Startup

1. `npm run build` — compile TypeScript
2. `npm run dev &` — start dev server on port 8090
3. `curl -s http://localhost:8090/health` — verify health
4. Check `tasks/lessons.md` for known fixes (L001-L016)

## Key Commands

```bash
npm run dev          # Dev server (port 8090)
npm run build        # Compile TypeScript
npm test             # Run 132 tests (vitest)
npm run test:watch   # Watch mode
npm run build:all    # Build TS + React SPA
```

## Architecture

- **Server:** `src/index.js` (Express 5, 24 API endpoints)
- **Schemas:** `src/schemas.ts` (Zod validation, TypeScript types)
- **Scoring:** `src/scoring.ts` (15-factor matching algorithm)
- **LLM:** `src/llm.ts` (Gemini 2.5 Flash — categorization, style signatures, embeddings)
- **Scrapers:** `scraper/` (14 Python BeautifulSoup scrapers)
- **Web UI:** `web/` (React 19 + Vite 7, 10 pages)
- **Database:** Firestore (`creators` collection, 38 docs, 768d embeddings)

## Coding Standards

- CommonJS (`require`/`module.exports`) for JS files
- `async/await` for all async ops; `const` default, `let` when needed
- Files: `kebab-case.js`, Functions: `camelCase`, Constants: `UPPER_SNAKE_CASE`
- API routes: `/api/v1/kebab-case`
- Validate inputs with Zod schemas before Firestore writes
- Never hardcode secrets; use `process.env`
- Emoji log prefixes: `📊`, `✅`, `❌`, `⚠️`, `🎬`
- Error responses: `{ success: false, error: message }`

## API Endpoints (Core)

```
POST /api/v1/match              — Match creators to brief (15-factor scoring)
POST /api/v1/search/semantic    — Embedding-based similarity search
GET  /api/v1/creators           — List/filter creators
POST /api/v1/creators           — Add creator
POST /api/v1/creators/batch     — Bulk import
POST /api/v1/categorize         — LLM-categorize a bio
POST /api/v1/scraper/trigger    — Run Python scraper pipeline
GET  /health                    — Health check
```

## Testing

- `tests/schemas.test.ts` — 45 tests (Zod validation)
- `tests/scoring.test.ts` — 41 tests (scoring algorithm)
- `tests/e2e-semantic.test.ts` — 33 tests (embedding pipeline)
- `tests/integration.test.ts` — 13 tests (requires running server)
- Config: `vitest.config.ts`, Coverage: V8 on `src/schemas.ts`, `src/scoring.ts`, `src/llm.ts`

## Deploy

```bash
gcloud run deploy cf-matching-engine --source . --region us-central1 \
  --project catchfire-app-2026 --no-allow-unauthenticated
# IMPORTANT: always use --update-env-vars, never --set-env-vars
```

## Auth

- Pre-approved to use any credentials in `~/Projects/`
- GCP: `charleys@johannesleonardo.com`, ADC quota: `catchfire-app-2026`
- GitHub: `cmscholz222`, Org: `Absolute-Space-GHCP`
- Production access: IAP-protected, 4 authorized users

## Key Files

| File | Purpose |
|------|---------|
| `src/schemas.ts` | Zod schemas, TypeScript types, validation helpers |
| `src/scoring.ts` | 15-factor scoring algorithm, keyword extraction |
| `src/llm.ts` | Gemini integration (categorize, style signatures, embeddings) |
| `src/index.js` | Express server, all API routes |
| `scraper/main.py` | Python scraper entry point (14 scrapers) |
| `src/services/scraper-transform.js` | Transform scraper output to creator schema |
| `tasks/todo.md` | Task tracker |
| `tasks/lessons.md` | Known fixes and lessons (L001-L016) |
| `vitest.config.ts` | Test configuration |

## Authorship

```
Author: Charley Scholz, JLAI
Co-authored: Claude Opus 4.6, Claude Code (coding assistant), Cursor (IDE)
```


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Mar 9, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #3393 | 10:28 AM | ✅ | Project Instructions: Created Comprehensive .claude/CLAUDE.md | ~527 |
</claude-mem-context>